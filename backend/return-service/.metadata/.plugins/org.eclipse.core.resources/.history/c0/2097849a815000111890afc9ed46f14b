package com.return_service.service;

import com.return_service.dto.*;

import java.util.List;

public interface ReturnService {

	ReturnResponseDTO processReturn(ProcessReturnRequestDTO request, CurrentUser currentUser);

    // 📄 Fetch single return
    ReturnResponseDTO getReturnById(String returnId,CurrentUser currentUser);
    
    List<ReturnResponseDTO> getReturnsByIssue(String issueId, CurrentUser currentUser);

    // 📄 Fetch list (filters later if needed)
    List<ReturnResponseDTO> getReturns(String requesterId, CurrentUser currentUser);
}